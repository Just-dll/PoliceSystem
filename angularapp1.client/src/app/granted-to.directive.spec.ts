import { GrantedToDirective } from './granted-to.directive';

describe('GrantedToDirective', () => {
  it('should create an instance', () => {
    const directive = new GrantedToDirective();
    expect(directive).toBeTruthy();
  });
});
